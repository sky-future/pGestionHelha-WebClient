import { UpdateLastConnexionPipe } from './update-last-connexion.pipe';

describe('UpdateLastConnexionPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateLastConnexionPipe();
    expect(pipe).toBeTruthy();
  });
});
