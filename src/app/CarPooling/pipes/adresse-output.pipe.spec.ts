import { AdresseOutputPipe } from './adresse-output.pipe';

describe('AdresseOutputPipe', () => {
  it('create an instance', () => {
    const pipe = new AdresseOutputPipe();
    expect(pipe).toBeTruthy();
  });
});
