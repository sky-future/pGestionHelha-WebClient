import { AddresseOutputPipe } from './addresse-output.pipe';

describe('AddresseOutputPipe', () => {
  it('create an instance', () => {
    const pipe = new AddresseOutputPipe();
    expect(pipe).toBeTruthy();
  });
});
