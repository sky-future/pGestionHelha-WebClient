import { ConfirmationPipe } from './confirmation.pipe';

describe('ConfirmationPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfirmationPipe();
    expect(pipe).toBeTruthy();
  });
});
